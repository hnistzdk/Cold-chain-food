<?xml version="1.0" encoding="UTF8" ?>
<!--究极之恶心的  如果xml文件的第一行的 encoding=UTF-8就会报错   改成UTF8才不会报错-->
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zdk.dao.UserMapper">
    <select id="login" resultType="user">
        select * from tb_user where username=#{username} and pwd=#{pwd};
    </select>
    <select id="queryFoodById" resultType="food">
        select * from tb_food where id=#{id};
    </select>
    <insert id="register" parameterType="java.lang.String">
        insert into tb_user
            (username, position, pwd, tel, register_date, last_login_time, login_times, real_name, gender)
            VALUES (#{username},null,#{pwd},null,null,null,null,null,null);
    </insert>
</mapper>